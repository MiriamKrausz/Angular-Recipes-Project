import { DifficultyLevelPipe } from './difficulty-level.pipe';

describe('DifficultyLevelPipe', () => {
  it('create an instance', () => {
    const pipe = new DifficultyLevelPipe();
    expect(pipe).toBeTruthy();
  });
});
